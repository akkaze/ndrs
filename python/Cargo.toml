[package]
name = "ndrs-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "_ndrs"
crate-type = ["cdylib"]

build = "build.rs"  # 指定 build.rs

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module", "abi3-py38"] }
numpy = "0.20"
ndrs = { path = "../ndrs" }