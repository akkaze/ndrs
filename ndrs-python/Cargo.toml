[package]
name = "ndrs-python"
version = "0.2.0"
edition = "2021"

[lib]
name = "_ndrs"
crate-type = ["cdylib"]

build = "build.rs"  # 指定 build.rs

[dependencies]
pyo3 = { version = "0.28", features = ["extension-module", "abi3-py38"] }
numpy = "0.28"
ndrs = { version = "0.2.0", path = "../ndrs" }