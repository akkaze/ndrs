[package]
edition = "2021"
name = "ndrs-python"
version = "0.4.0"

[lib]
crate-type = ["cdylib"]
name = "_ndrs"

[dependencies]
ndrs = {version = "0.4.0", path = "../ndrs"}
numpy = "0.28"
pyo3 = {version = "0.28", features = ["extension-module", "abi3"]}
